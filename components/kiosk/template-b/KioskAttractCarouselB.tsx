/**
 * Retained as a thin alias. The idle/attract screen — a full-bleed idle-media
 * cross-fade carousel with a branded fallback — is now shared by every template
 * in `KioskAttractScreen`. Kept so existing imports keep resolving.
 */
export { KioskAttractScreen as KioskAttractCarouselB } from "@/components/kiosk/KioskAttractScreen";
